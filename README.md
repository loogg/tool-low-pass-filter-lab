# LPF·LAB：一阶低通滤波交互实验室

一个面向初学者和嵌入式开发者的纯前端教学工具。它把一阶低通滤波器的三个常见参数——时间常数 `τ`、截止频率 `fc`、数字系数 `α`——放进同一套可交互实验中，帮助用户从公式建立到工程选型，再落到 MCU 代码。

## 主要功能

- 五个一级教学工作区改为桌面端左侧竖向导航：`直觉 / 响应 / 模拟 / 选型 / 代码` 点击切换，不再把所有章节堆成一张超长页面；手机端自动转为横向可滑动导航。
- 全局 `fc` 与 `fs` 独立设置：`fc` 支持 `1 mHz ～ 50 MHz`，`fs` 支持 `1 Hz ～ 200 MHz`，数值输入可切换 `Hz / kHz / MHz`。
- 响应与代码工作区采用固定的“左侧纵向参数、右侧分析结果”布局；时域、幅频、相频和代码始终保留最大可读宽度。
- 普通计算结果区同时列出数字实现、时域响应、幅相响应和实时计算负载，并用具体差值示例解释 `α` 的含义。
- 直觉工作区的采样边界区给出 `fN = fs/2`、混叠通用公式与四个 Nyquist 区的正向/镜像折尺。
- 阶跃响应横轴以 `1τ ～ 5τ` 标注，配合幅频响应和相频响应原生 SVG 图表建立直觉。
- 幅频图直接标注 `−3 dB ≈ 0.707×`、`−20 dB ≈ 1/10`、`−40 dB ≈ 1/100`，并区分幅值比与功率比公式。
- 仿真器重构为专业工作台：五个二级功能 Tab 横向排列，下面固定为左侧参数编辑器、右侧波形与计算结果，避免与一级导航形成两套竖向 Tab。
- 五个二级 Tab 分别管理模拟信号、滤波器、噪声干扰、采样与混叠、观察与计算；高级参数全部保留，但不会再同时铺满页面。
- 仿真器拥有独立的 `fc`、`fs` 与离散方法，也可一键载入全局参数后继续单独调整。
- 正弦、方波、阶跃三类输入全部开放工程参数：频率、幅值、偏置、相位、占空比、阶跃电平与时刻、噪声种子、周期干扰、递推初值和观察窗口等。
- 主信号与周期干扰频率支持 `1 mHz ～ 2fs`，可以跨越 4 个 Nyquist 区，并提供 `0.6fs / 0.9fs / 1.1fs / 1.6fs` 一键折回示例。
- 仿真器同时绘制采样前模拟参考波形、ADC 样本和数字低通输出；超过 `fN` 时，时间曲线直接显示原频率如何表现为 `f_alias`。
- Nyquist 页不再用抽象三角折线作为主解释，而是按“画出 `fN` → 对 `fin` 取余 → 必要时镜像 → 得到 `f_alias`”逐步展示，并配两条频率尺、四区方向规律和一键例题。
- 仿真结果区分 ADC 前模拟原型在原始频率处的响应，以及 ADC 后数字滤波器在混叠频率处的实际响应。
- 仿真结果还输出 dB、相位、群时延、时间常数、真实采样点数、递推步数、绘图点数、RMS 与实时运算速率。
- MHz 级采样采用明确标注的等效分块递推，真实参数和计算负载仍按设置的 `fs` 计算，避免浏览器因数亿次循环冻结。
- ZOH 精确离散与后向欧拉系数对比。
- 时域、通带、阻带三类设计约束求交，可判断一阶滤波器是否可行。
- 自动生成可复制的嵌入式 C 实现。
- 响应式布局、键盘可操作控件和减少动态效果支持。

## 核心关系

```text
τ = 1 / (2πfc)

y[n] = y[n-1] + α(x[n] - y[n-1])

ZOH:             α = 1 - exp(-2πfc / fs)
Backward Euler:  α = 2πfc / (fs + 2πfc)

fN = fs / 2
r = fin mod fs
falias = min(r, fs - r)
```

## 本地运行

要求 Node.js 20 或更高版本。

```bash
npm install
npm run dev
```

浏览器打开终端显示的本地地址即可。

## 质量检查

```bash
npm run lint
npm test
npm run build
```

数学单元测试覆盖参数换算、截止点幅相特性、两种离散系数、Nyquist 区折回、混叠后的数字幅相等价性、约束求解、阶跃收敛、信号细节参数和 MHz 级采样的有界递推。

## GitHub Pages

在线体验：[https://loogg.github.io/tool-low-pass-filter-lab/](https://loogg.github.io/tool-low-pass-filter-lab/)

仓库包含 GitHub Actions 工作流。推送与 `package.json` 版本一致的语义化标签即可构建并部署：

```bash
npm version patch -m "chore(release): v%s"
git push origin main --follow-tags
```

普通提交只运行 CI，不触发 Pages 发布。

## 技术栈

- React 19
- Vite 7
- 原生 SVG 图表
- Vitest
- Lucide React

## 设计原则

- 先给直觉，再给公式。
- 每个数字都配即时工程解释。
- 不把 `fc` 描述成硬边界。
- 不把 `fs/2` 描述成“超过后消失”：明确展示相邻 Nyquist 区会交替镜像折回。
- 不把后向欧拉误称为 Tustin。
- 明确一阶滤波器、抗混叠和离群值处理的能力边界。
